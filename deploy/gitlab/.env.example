# GitLab 密钥与配置 — chmod 600，不进 git（仅 .env.example 进版本控制）
GITLAB_DB_PASSWORD=__CHANGE_ME__
GITLAB_ROOT_PASSWORD=__CHANGE_ME__
# SMTP（按需）
SMTP_ENABLED=false
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
